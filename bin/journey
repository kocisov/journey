#!/usr/bin/env node

const journey = require('./journey.bundle')

console.log(process.argv)

if (process.argv[2]) {
  const projectName = process.argv[2]
  const commands = process.argv[3]

  journey(projectName, commands && commands)
}